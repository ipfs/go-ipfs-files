package files

import (
	"github.com/ipfs/go-libipfs/files"
)

var FileEntry = files.FileEntry

type SliceFile = files.SliceFile

var NewMapDirectory = files.NewMapDirectory
var NewSliceDirectory = files.NewSliceDirectory
